#ifndef ABS(X)
#define ABS(X)

/**
 * ABS- 'Function that computes absolute value of a number'
 * @X:- Member
 * Return: 0
 */

int ABS(int X)
{
	return ((X < 0) ? -(X) : (X));
}

#endif /* ABS(X) */
