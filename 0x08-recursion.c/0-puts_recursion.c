#include "main.h"

/**
 * _puts_recursion- Entry Point
 *
 * Description:- 'A function that prints a string'
 * @s:- string to print
 *
 * Return: 0 (Success)
 */

void _puts_recursion(char *s)
{
	int i;

	for (i = 0; i <= s[i]; i++)
	{
		_putchar(s[i]);
	}
}
