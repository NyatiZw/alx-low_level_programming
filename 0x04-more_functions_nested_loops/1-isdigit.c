# include "main.h"
/**
 * _isdigit- function to find is character is Uppercase
 *
 * Return: 1 if digit 0 otherwise
 */


_isdigit(void)
{
	int i;

	for (i = 0; i <= 10; i++)
	{
		return (1);
	}

	return (0);
}

