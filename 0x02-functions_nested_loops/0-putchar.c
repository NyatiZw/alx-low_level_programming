# include "main.h"
/**
 * main - Entry Point
 *
 * Description: "C functions & nested loops"
 *
 * Return: Always 0 (Success)
 *
 */

int main(void)
{
	int ch = '_';
	int p = 'p';
	int u = 'u';
	int t = 't';
	int c = 'c';
	int h = 'h';
	int a = 'a';
	int r = 'r';

	putchar(ch);
	putchar(p);
	putchar(u);
	putchar(t);
	putchar(c);
	putchar(h);
	putchar(a);
	putchar(r);
	putchar('\n');

	return (0);

}
