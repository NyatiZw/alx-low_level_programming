# include "main.h"

/**
 * main - entry point
 *
 * Description: "c functions & nested loops"
 *
 * Return: Always 0 (Success)
 *
 */

int main(void)
{
	int ch;

	for (ch = 'a'; ch <= 'z'; ch++)

	putchar(ch);
	putchar('\n');

	return (0);
}
