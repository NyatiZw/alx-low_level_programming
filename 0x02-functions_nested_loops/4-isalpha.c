# include "main.h"

/**
 * islower - entry point
 *
 * description: 'function _islower()'
 *
 * Return: 1 (Success) || 0 (Not)
 *
 */

void isalpha(void)
{
	int c;

	if ( _isalpha(c))
		_putchar('1');
		else _putchar('0');

		return (1);
}
