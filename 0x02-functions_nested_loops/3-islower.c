# include "main.h"

/**
 * islower - entry point
 *
 * description: 'function _islower()'
 *
 * Return: 1 (Success) || 0 (Not)
 *
 */

int islower(void)
{
	int c;

	res = islower(c);

	if (c >= 'a' && c <= 'z')
		putchar('1');
	else putchar('0');

		return (1);
}
