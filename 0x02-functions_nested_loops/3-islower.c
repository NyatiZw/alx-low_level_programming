# include "main.h"

/**
 * islower - entry point
 *
 * description: 'function _islower()'
 *
 * Return: 1 (Success) || 0 (Not)
 *
 */

void _islower(void)
{
	int c;

	if (islower(c))
		putchar('1');
		else putchar('0');

		return (1);
}
